main(){
  var name = "Tushar";
  var full_name = "Tushar Dimri";
   var $my$salary_ = 1000000000000000000;

   // A variable name cannot 'begin' with a number
   // Numbers can be placed in any place except the first index
   // Spaces, tabs , newlines cannot be added between variable names
   //  We can use underscore(_) or dollar($) between or at the beginning variable names
}

// Variable named with start with _ are private variables and cannot be used outside their block